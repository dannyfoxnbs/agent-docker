# Nimbus

## Workspace

The main product workspace is `nx/`.

Run all Nx commands from `nx/`.

`nimbus-spec-import/` is a separate standalone package and is not part of the Nx workspace.

Use `nx/AGENTS.md` for Nx workspace commands and verification steps.
